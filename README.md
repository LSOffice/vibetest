# vibetest

pentesting for vibecoded apps

## Installation

```bash
npm install
npm run build
```

## Usage

```bash
# Using npm start (note the -- to pass arguments)
npm start -- --port 3000

# Or run directly
node dist/index.js --port 3000

# Development mode
npm run dev -- --port 3000
```
